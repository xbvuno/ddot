[workspace]
members = [
    "core",
    "cli",
    "wasm", 
    "core-macros",
]

resolver = "2"

[profile.release]
codegen-units = 1
lto = "fat"
opt-level = 3
panic = "abort"
strip = "symbols"

[profile.release.package.ddot-wasm]
opt-level = "z"
